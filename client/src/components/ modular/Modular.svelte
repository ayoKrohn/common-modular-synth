<script>
    let AudioContext = window.AudioContext || window.webkitAudioContext;

    //we build the audio context for our sound (like a canvas for visual art)
    const context = new AudioContext;

    


</script>